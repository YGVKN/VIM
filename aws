aws configure set aws_access_key_id  --profile default && aws configure set --profile default && aws_secret_access_key  --profile default && aws configure set region  --profile default && aws configure set output json --profile defaul
