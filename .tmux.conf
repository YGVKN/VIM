set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
#set -g @plugin 'jimeh/tmux-themepack'
#set -g @plugin 'dracula/tmux'

set-option -g status on
set -g status-interval 1
set -g status-justify centre
set-window-option -g clock-mode-style 24
set-option -g status-style fg=colour136,bg=colour235
set-option -g status-left-length 111
set-option -g status-left-style default
set -g status-left "#[fg=magenta]: #H : #[fg=black]• #[fg=magenta,bright]#(curl icanhazip.com) #(ifconfig en0 | grep 'inet ' | awk '{print \"en0 \" $2}') #(ifconfig en1 | grep 'inet ' | awk '{print \"en1 \" $2}') #(ifconfig en3 | grep 'inet ' | awk '{print \"en3 \" $2}') #(ifconfig tun0 | grep 'inet ' | awk '{print \"vpn \" $2}') #(uname -s -m)"
#set-option -g status-left "#[fg=magenta]#H #[fg=black]• #[fg=magenta,bright]#(uname -s -m -r)#[default]"
set-option -g status-right-length 140
set-option -g status-right-style default
set-option -g status-right "#[fg=magenta,bg=default,bright]#(tmux-mem-cpu-load) "
set-option -ag status-right "#[fg=red,dim,bg=default]#(uptime | cut -f 4-5 -d ' ' | cut -f 1 -d ',') "

set-option -ag status-right " #[fg=white,bg=default]%a, %e %b %Y #[fg=magenta]%H:%M:%S %z#[default]"
set-window-option -g window-status-style fg=colour244
set-window-option -g window-status-style bg=default
set-window-option -g window-status-current-style fg=colour166
set-window-option -g window-status-current-style bg=default
# available plugins: battery, cpu-usage, git, gpu-usage, ram-usage, tmux-ram-usage, network, network-bandwidth, network-ping, attached-clients, network-vpn, weather, time, spotify-tui, kubernetes-context, synchronize-panes

#set -g @dracula-plugins "cpu-usage  ram-usage battery time"

# available colors: white, gray, dark_gray, light_purple, dark_purple, cyan, green, orange, red, pink, yellow
# set -g @dracula-[plugin-name]-colors "[background] [foreground]"

#set -g @dracula-cpu-usage-colors "pink dark_gray"
#set -g @dracula-ram-usage-colors "pink dark_purple"
#set -g @dracula-network-ping-colors "cyan dark_purple"
#set -g @dracula-show-powerline true
## for left
#set -g @dracula-show-left-sep λ
#
## for right symbol (can set any symbol you like as seperator)
#set -g @dracula-show-right-sep  λ

#set -g @dracula-show-flags true
# the default is 5, it can accept any number
#set -g @dracula-refresh-rate 5
#set -g @dracula-border-contrast true
#set -g @dracula-cpu-usage-label "CPU"
#set -g @dracula-cpu-display-load true
#set -g @dracula-battery-label "Battery"
#set -g @dracula-ram-usage-label "RAM"
#set -g @dracula-tmux-ram-usage-label "MEM"
#set -g @dracula-network-bandwidth eth0
#set -g @dracula-network-bandwidth-interval 0
#set -g @dracula-network-bandwidth-show-interface true
#set -g @dracula-ping-server "waww.io"
#set -g @dracula-ping-rate 5
#set -g @dracula-day-month true
#set -g @dracula-military-time true
#set -g @dracula-git-disable-status true
# default is ✓. Avoid using non unicode characters that bash uses like $, * and !
#set -g @dracula-git-show-current-symbol ✓
## default is unicode no message
#set -g @dracula-git-no-repo-message ""
## default is false
#set -g @dracula-git-no-untracked-files true
## default is false
#set -g @dracula-git-show-remote-status true
#set -g @dracula-show-location false
set -g default-terminal "xterm-256color"
run '~/.tmux/plugins/tpm/tpm'
#https://foragoodstrftime.com/  date format
